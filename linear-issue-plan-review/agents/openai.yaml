interface:
  display_name: "Linear Issue Plan Review"
  short_description: "Draft and review a Linear Issue plan"
  default_prompt: "Use $linear-issue-plan-review with the specified Linear Issue ID to refine its existing canonical Plan against the Repository, or create one when absent, then review and update it."

policy:
  allow_implicit_invocation: false
