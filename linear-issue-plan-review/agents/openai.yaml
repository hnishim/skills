interface:
  display_name: "Linear Issue Plan Review"
  short_description: "Draft and review a Linear Issue plan"
  default_prompt: "Use $linear-issue-plan-review with the specified Linear Issue ID to draft, review, and update its plan."

policy:
  allow_implicit_invocation: false
