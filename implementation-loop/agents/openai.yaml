interface:
  display_name: "Implementation Loop"
  short_description: "Run an approved plan through test-first implementation"
  default_prompt: "Use $implementation-loop to create and review acceptance tests before implementing the approved plan through bounded review cycles."

policy:
  allow_implicit_invocation: false
