interface:
  display_name: "Implementation Loop"
  short_description: "Run an approved plan through implementation and review"
  default_prompt: "Use $implementation-loop to execute the approved plan through implementation, review, and bounded fixes."
